import { BubbleGraphDirective } from './bubble-graph.directive';

describe('BubbleGraphDirective', () => {
  it('should create an instance', () => {
    const directive = new BubbleGraphDirective();
    expect(directive).toBeTruthy();
  });
});
